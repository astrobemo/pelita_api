version: '3'

services:
  backend:
    image: pelita_api_services
    build: .
    ports:
      - "3000:3000"
    command: ["node", "index.js"]
    env_file:
      - .env
      - .env.development
    network_mode: "host"
  